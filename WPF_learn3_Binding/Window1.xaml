<Window x:Class="WPF_learn3_Binding.Window1"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPF6_Binding"
        xmlns:model="clr-namespace:WPF6_Binding.Model"
        mc:Ignorable="d"
        Title="Window1" Height="450" Width="800">
    <!--<StackPanel Background="Blue">
        <StackPanel.DataContext>
            <model:Student Id="1" Name="John" Age="20" />
        </StackPanel.DataContext>
        <Grid>
            <StackPanel>
                <TextBox Text="{Binding Id}" />
                <TextBox Text="{Binding Name}" />
                <TextBox Text="{Binding Age}" />
            </StackPanel>
        </Grid>
    </StackPanel>-->
    <!--<StackPanel>
        <ListView x:Name="listView">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Id" Width="80" DisplayMemberBinding="{Binding XPath=@Id}" />
                    <GridViewColumn Header="Name" Width="80" DisplayMemberBinding="{Binding XPath=Name}" />
                </GridView>
            </ListView.View>
        </ListView>
    </StackPanel>-->
</Window>
